# ===========================================
# INCIDENT MANAGEMENT SYSTEM CONFIGURATION
# ===========================================

# Server Configuration
PORT=5000
APP_BASE_URL=https://your-app.replit.dev

# Database
DATABASE_URL=postgresql://user:password@host:port/database

# Authentication & Security
JWT_SECRET=your-super-secret-32-character-jwt-key-here-change-this

# RBAC Roles (comma-separated)
ROLES=Reporter,Analyst,Approver,Admin

# SLA Configuration
SLA_PROFILE_STANDARD_HOURS=24

# SMTP Configuration for Notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@company.com
SMTP_PASS=your-email-app-password
MAIL_FROM=incidents@company.com

# Optional: Redis for Caching
REDIS_URL=redis://localhost:6379

# Optional: Dashboard Integration
DASHBOARD_URL=https://your-dashboard.company.com
DASHBOARD_API_KEY=your-dashboard-api-key

# Evidence Storage Configuration
DEFAULT_STORAGE_MODE=pointer
ALLOW_POINTER=true
ALLOW_MANAGED_COPY=true
CACHE_TTL_SECONDS=0
BYO_STORAGE_PROVIDER=s3

# Optional: S3 Configuration (if BYO_STORAGE_PROVIDER=s3)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
S3_BUCKET=your-evidence-bucket

# ===========================================
# USAGE EXAMPLES:
# ===========================================
# 
# Reporter Role:
#   - Can create incidents they reported
#   - Can view/edit their own incidents
#   - Can add evidence to incidents
#
# Analyst Role:
#   - Can initiate workflows
#   - Can add stakeholders
#   - Can toggle notifications
#   - Can access /workflow/integration
#   - Can view all incidents
#
# Approver Role:
#   - Can approve/deny workflows when required
#   - Can view all incidents and workflows
#
# Admin Role:
#   - Full system access
#   - Can manage presets and integrations
#   - Not required to initiate workflows
#
# ===========================================